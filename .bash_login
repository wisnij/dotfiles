_bash_login () {
    if [[ -f $HOME/.bashrc ]]; then
        source $HOME/.bashrc
    fi

    if [[ -d $HOME/.config/bash/login ]]; then
        local file
        for file in $HOME/.config/bash/login/*.bash; do
            source "$file"
        done
    fi
}

_bash_login
