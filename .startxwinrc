#!/bin/sh

xrdb .Xresources
xhost +

if type -t tmux >/dev/null; then
    tmux="-e tmux"
fi
urxvt -ls $tmux &

if [[ -f .startxwinrc-local ]]; then
    source .startxwinrc-local
fi

exec /usr/bin/xwin-xdg-menu
