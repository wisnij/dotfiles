To show a list of users at the login dialog:

    # /etc/lightdm/lightdm.conf
    greeter-hide-users=false

To adjust the login screen for 4K monitor scaling:

    # /etc/lightdm/lightdm-gtk-greeter.conf
    xft-dpi=192
